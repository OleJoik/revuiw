services:
  dev:
    build: .
    container_name: revuiw-dev
    working_dir: /workspace
    volumes:
      - .:/workspace
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    command: bash entrypoint.sh
